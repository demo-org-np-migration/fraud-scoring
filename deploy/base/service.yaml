apiVersion: v1
kind: Service
metadata:
  name: fraud-scoring
  labels:
    app.kubernetes.io/name: fraud-scoring
    app.kubernetes.io/part-of: cauri
    cauri.io/team: risk
spec:
  selector:
    app.kubernetes.io/name: fraud-scoring
  ports:
    - name: http
      port: 8080
      targetPort: http
